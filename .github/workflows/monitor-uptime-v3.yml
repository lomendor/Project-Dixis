name: Monitor Uptime v3

on:
  workflow_dispatch:
  # DISABLED: redundant — consolidated into uptime-monitor.yml + production-smoke.yml
  # schedule:
  #   - cron: "*/10 * * * *"

jobs:
  prod-http-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Healthz
        run: curl -fsS https://dixis.gr/api/healthz --connect-timeout 10 --max-time 15 -4 >/dev/null
