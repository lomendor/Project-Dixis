/**
 * Lightweight smoke test—runs only in CI with dev server.
 * If not applicable in current setup, keep as placeholder.
 */
describe('CSP header smoke', () => {
  it('placeholder', () => {
    expect(true).toBe(true)
  })
})