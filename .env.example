# Root .env.example (pointer)
# Παρακαλώ χρησιμοποίησε το canonical αρχείο:
#   → frontend/.env.example
#
# Για CI, το DATABASE_URL ορίζεται στο workflow env.

# --- SMTP (optional: emails no-op when missing) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Dixis <no-reply@dixis.gr>"
DEV_MAIL_TO=
